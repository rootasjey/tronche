# Session encryption (required, generate with: openssl rand -base64 32)
NUXT_SESSION_PASSWORD=

# Auth origin (used for OAuth callbacks)
NUXT_AUTH_ORIGIN=http://localhost:3000

# Zima Blue API key for fetching gallery images (zb_ prefix)
NUXT_ZIMA_BLUE_API_KEY=
